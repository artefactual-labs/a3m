[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[tool.ruff]
target-version = "py39"
select = ["E", "F", "W"]
ignore = [
  "E402",
  "E501",
  "E722",
]
exclude = [
  "a3m/api/**/*",
]
